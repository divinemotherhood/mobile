import { fontScale } from "./responsive";

export const typography = {
    h1: fontScale(38),
    h2: fontScale(26),
    h3: fontScale(20),
    body: fontScale(16),
    caption: fontScale(12),
    regular: fontScale(14),
    small: fontScale(10),
};